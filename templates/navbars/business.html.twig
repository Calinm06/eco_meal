<nav class="navbar navbar-expand-lg navbar-light bg-light p-3">
    <a class="navbar-brand" href="{{ path('app_business_view', {id: app.user.business.id}) }}">
            <span class="fs-3 fw-bolder text-primary tracking-wide">
    EcoMeal<span class="text-secondary-50"></span></span>
    </a>

    <div class="collapse navbar-collapse">
        <div class="navbar-nav ms-auto">

            <a class="nav-item nav-link active" href="{{ path('app_package_new', {id: app.user.business.id}) }}">Create package</a>
            <a class="nav-item nav-link active ms-3" href="{{ path('app_business_packages',{id: app.user.business.id}) }}">Packages</a>
            <a class="nav-item nav-link active ms-3" href="{{ path('app_business_statistics',{id: app.user.business.id}) }}">Statistics</a>

            <div class="nav-item dropdown ms-4">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                    <img src="{{ asset('Icons/user.png') }}" alt="profile" width="32" height="32">
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
{#                    <li><a class="dropdown-item" href="{{ path('app_consumer_edit', {id: app.user.consumer.id}) }}">Edit profile</a></li>#}
{#                    <li><hr class="dropdown-divider"></li>#}
                    <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Logout</a></li>
                </ul>
            </div>
        </div>
    </div>
</nav>
