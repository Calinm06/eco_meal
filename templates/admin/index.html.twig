{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 1500px; width: 95%; font: 18px/1.5 sans-serif; }
    /*.example-wrapper code { background: #FFFFFF; padding: 2px 6px; }*/
</style>

<div class="example-wrapper">
    <a class="btn btn-primary", href="{{ path('app_business') }}">See all business</a>
    <a class="btn btn-primary", href="{{ path('app_business_new') }}">Create a business</a>
    <a class="btn btn-primary", href="{{ path('app_consumer') }}">See all consumers</a>
    <a class="btn btn-primary", href="{{ path('app_business_type') }}">Manage business types</a>
    <a class="btn btn-primary", href="{{ path('app_category') }}">Manage categories</a>
    <a class="btn btn-primary", href="{{ path('app_package') }}">See all packages</a>
</div>
{% endblock %}
